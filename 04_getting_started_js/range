var arr = [];
var range = function(from, to){
    if(from<=to){
        let tempArr = [];
        tempArr[0] = from;
        arr = arr.concat(tempArr);
        range(from+1, to);
    }
    return arr;
}

console.log(range(1, 10));